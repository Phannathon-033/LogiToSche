# Security Token for Gateway (Port 8000)
# If set, external clients must send header 'X-LogiAI-Token' matching this value.
# Leave empty to disable token requirement (open dev mode).
LOGIAI_GATEWAY_TOKEN=
